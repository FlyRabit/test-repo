# AGENTS.md

## Cursor Cloud specific instructions

This is a minimal/skeleton repository containing only a single `HelloWorld.md` file. There is no application code, build system, test framework, or dependency management.

### Repository contents

- `HelloWorld.md` — placeholder markdown file

### Development notes

- No dependencies to install (no `package.json`, `requirements.txt`, or similar).
- No linting, testing, or build tooling configured.
- No services to start or manage.
- The update script is a no-op (`echo "No dependencies to install"`), since there are no dependencies.
- If application code is added in the future, update this file and the VM update script accordingly.
